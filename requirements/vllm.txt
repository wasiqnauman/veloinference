# Runtime-only requirements for the GPU experiment environment.
#
# Keep these separate from the CPU-safe project lockfile. The vLLM environment
# should use its own Python 3.12 virtual environment on Linux or WSL2.
vllm==0.29.0
ninja==1.13.2

# Pin the compiler/runtime pair used in the published experiment.
nvidia-cuda-nvcc==13.4.59
nvidia-cuda-runtime==13.4.49
flashinfer-python==0.6.18
