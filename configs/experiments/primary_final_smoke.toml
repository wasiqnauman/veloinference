# Purpose: exercise every EXP-003 service path before the full GPU matrix.
name = "exp003-primary-smoke"
model_config = "../models/qwen2_5_1_5b.toml"
workload_config = "../workloads/final_smoke.toml"
target_endpoint = "adip"
adip_gateway_mode = "batched"
policy = "fixed"
max_batch_size = 8
max_wait_ms = 1
adaptive_max_wait_ms = 20
repetition_seeds = [1729]
cooldown_s = 0.5
output_dir = "../../results/raw"
slo_derivation_rule = "smoke_only_no_claim"
health_url = "http://127.0.0.1:8000/health"
final_modes = ["direct", "pass_through", "fixed", "adaptive"]
final_rates_rps = [0.5]
final_repetitions = [1]
harness_drift_threshold_ms = 50.0
harness_error_threshold = 0.01
