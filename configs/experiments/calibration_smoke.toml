# Purpose: validate EXP-001 wiring without producing a capacity claim.
name = "exp001-calibration-smoke"
model_config = "../models/qwen2_5_1_5b.toml"
workload_config = "../workloads/calibration_smoke.toml"
target_endpoint = "direct"
adip_gateway_mode = "pass_through"
policy = "fixed"
max_batch_size = 8
max_wait_ms = 0
repetition_seeds = [1729]
cooldown_s = 0.0
output_dir = "../../results/raw"
slo_derivation_rule = "not_for_capacity_claims"
health_url = "http://127.0.0.1:8001/health"
calibration_rates_rps = [1.0]
harness_drift_threshold_ms = 50.0
harness_error_threshold = 0.01
