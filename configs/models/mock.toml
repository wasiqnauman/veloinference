# Purpose: small local-only model contract used by benchmark parser tests and smoke runs.
model_id = "mock"
served_name = "mock"
revision = "local"
dtype = "float32"
max_model_len = 2048
gpu_memory_utilization = 0.50
max_num_seqs = 8
temperature = 0.0
max_tokens = 8
